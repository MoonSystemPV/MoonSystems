---
const OFFER = {
  name: "Página Web + Dominio",
  price: "350.000",
  originalPrice: "450.000",
  description: "¡Oferta especial por tiempo limitado!",
  features: [
    "Diseño Ultra Moderno & Responsivo",
    "Dominio .COM Premium (1 año)",
    "SEO Optimizado para Google",
    "Sistema de Contacto Avanzado",
    "Redes Sociales Integradas",
    
  ]
};
---

<section id="precios" class="relative py-20 bg-gradient-to-b from-slate-50 to-white dark:from-slate-900 dark:to-slate-800 overflow-hidden">
  {/* Fondo animado SOLO para esta sección */}
  <div class="pointer-events-none absolute inset-0 z-0 bg-gradient-to-tr from-cyan-100/60 via-blue-100/40 to-white dark:from-cyan-900/60 dark:via-blue-900/40 dark:to-slate-900"></div>

  {/* Contador solo aquí */}
  <div id="oferta-countdown" class="fixed top-6 right-6 z-20 bg-white/90 dark:bg-slate-900/90 backdrop-blur px-6 py-3 rounded-xl border border-cyan-200 dark:border-cyan-700 text-cyan-600 dark:text-cyan-400 font-bold text-lg shadow-lg flex items-center gap-2">
    <span>⏰ Quedan:</span>
    <span id="timer">23:47:15</span>
  </div>

  <div class="container flex items-center justify-center min-h-[60vh] relative z-10">
    <div class="card relative rounded-3xl shadow-2xl border border-cyan-200 dark:border-cyan-700 max-w-3xl w-full overflow-hidden animate-float">
      {/* Fondo animado parpadeante */}
      <div class="absolute inset-0 z-0 animate-pulse bg-gradient-to-br from-cyan-200 via-cyan-100 to-white dark:from-cyan-900 dark:via-blue-900 dark:to-slate-900 opacity-80"></div>
      <div class="header px-6 pt-8 pb-6 text-center relative z-10">
        <div class="inline-flex items-center gap-2 bg-cyan-100 border border-cyan-300 px-6 py-2 rounded-full text-cyan-600 font-semibold text-sm mb-4 animate-pulse">
          <span class="text-lg">⚡</span> Oferta Explosiva - Solo Hoy
        </div>
        <h1 class="main-title text-4xl md:text-6xl font-extrabold bg-gradient-to-r from-cyan-400 via-blue-400 to-cyan-600 bg-clip-text text-transparent mb-2 drop-shadow-lg animate-glow">
          MEGA OFERTA
        </h1>
        <p class="subtitle text-base md:text-lg text-slate-600 dark:text-slate-300 font-light max-w-xl mx-auto mb-4">
          Transforma tu negocio con una presencia web que realmente impacta y convierte
        </p>
      </div>
      <div class="content-grid grid md:grid-cols-2 gap-0">
        {/* Precio y CTA */}
        <div class="pricing-hero flex flex-col items-center justify-center bg-transparent px-6 py-8 relative z-10">
          <h2 class="package-title text-xl font-bold text-cyan-700 dark:text-cyan-300 mb-4">Web + Dominio Premium</h2>
          <div class="price-display text-center mb-6">
            <div class="mega-price text-5xl md:text-6xl font-extrabold text-cyan-600 dark:text-cyan-400 mb-1 drop-shadow-xl">350<span class="align-top text-xl">K</span></div>
            <div class="old-price text-lg text-slate-400 line-through mb-1">450.000 CLP</div>
            <div class="save-amount inline-block bg-gradient-to-r from-cyan-400 to-blue-500 text-white px-4 py-1 rounded-full font-bold shadow-lg text-sm">🔥 AHORRAS 100K</div>
          </div>
          <button 
            type="button"
            onclick="const select = document.getElementById('subject'); select.value='Oferta Especial - Página Web + Dominio'; select.dispatchEvent(new Event('change')); document.getElementById('contacto').scrollIntoView({behavior: 'smooth'});"
            class="cta-button inline-block w-full md:w-auto bg-cyan-600 hover:bg-cyan-700 text-white font-bold py-3 px-6 rounded-xl transition-all duration-300 transform hover:scale-105 hover:shadow-lg shadow-md text-center uppercase tracking-wider"
          >
            ¡Aprovecha Ahora!
          </button>
        </div>
        {/* Características */}
        <div class="features-section bg-white/70 dark:bg-slate-900/50 px-6 py-8 flex flex-col justify-center rounded-b-3xl z-10">
          <h3 class="features-title text-lg font-bold text-cyan-700 dark:text-cyan-300 mb-6 flex items-center gap-2">💎 Todo Incluido Premium:</h3>
          <ul class="features-list space-y-4">
            {OFFER.features.map((feature, i) => (
              <li class="feature flex items-center group">
                <div class="feature-icon w-10 h-10 flex items-center justify-center rounded-full bg-gradient-to-br from-cyan-400 to-blue-400 text-white text-xl shadow-lg animate-pulse mr-3">
                  {['🚀','🌐','📈','💬','📱'][i]}
                </div>
                <span class="feature-text text-slate-700 dark:text-slate-200 text-base font-medium">{feature}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </div>

  <style>
    .animate-float {
      animation: cardFloat 6s ease-in-out infinite;
    }
    @keyframes cardFloat {
      0%, 100% { transform: translateY(0) rotate(0deg); }
      50% { transform: translateY(-10px) rotate(0.5deg); }
    }
    .animate-glow {
      animation: titleGlow 3s ease-in-out infinite alternate;
    }
    @keyframes titleGlow {
      0% { filter: drop-shadow(0 0 20px rgba(6,182,212,0.3)); }
      100% { filter: drop-shadow(0 0 40px rgba(6,182,212,0.6)); }
    }
  </style>

  <script>
    if (typeof window !== 'undefined') {
      function updateCountdown() {
        const now = new Date();
        const endOfDay = new Date();
        endOfDay.setHours(23, 59, 59, 999);
        const timeDiff = endOfDay.getTime() - now.getTime();
        const hours = Math.floor(timeDiff / (1000 * 60 * 60));
        const minutes = Math.floor((timeDiff % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((timeDiff % (1000 * 60)) / 1000);
        const timer = document.getElementById('timer');
        if (timer) {
          timer.textContent = `${hours.toString().padStart(2, '0')}:${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
        }
      }
      updateCountdown();
      setInterval(updateCountdown, 1000);
    }
  </script>
</section>

<style>
  @keyframes float {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
    100% { transform: translateY(0px); }
  }

  [data-animate] {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.6s ease forwards;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .section {
    position: relative;
  }

  .section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 100%;
    background: radial-gradient(circle at top right, rgba(99, 102, 241, 0.1), transparent 70%);
    pointer-events: none;
  }
</style> 